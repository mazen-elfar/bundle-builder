import VariantChip from "./VariantChip";

const VariantSelector = () => {
  return (
    <section>
      <VariantChip />

      <VariantChip />

      <VariantChip />
    </section>
  );
};

export default VariantSelector;